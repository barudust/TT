version: "3"
authtoken: YOUR_NGROK_AUTH_TOKEN_HERE

tunnels:
  api:
    proto: http
    addr: 8000
    # Opcional: si tienes un plan pagado, puedes usar dominio personalizado
    # domain: api-trading.ngrok-free.app

  app:
    proto: http
    addr: 5173
    # Opcional: si tienes un plan pagado, puedes usar dominio personalizado
    # domain: app-trading.ngrok-free.app
